 <div class="container">
 <div class="row">
   <div class="col-md-8 offset-md-1">
     <h2>会員一覧</h2>
   <table class="table">
   <% @customers.each do |user| %>
   <thead>
    <tr>
    <th><%= link_to "Show", customers_path(user) %></th>
    </tr>
   </thead>
   <% end %>
 </div>
 </div>
 </div>